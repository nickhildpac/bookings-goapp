{{template "base" .}}

{{define "content"}}
  <div class="container">
    <div class="row">
      <div class="col">
        <h1> this is about page</h1>
        <p> this is about content </p>
        <p> this is about content </p>
        <p> this is from template {{index .StringMap "test"}} </p>
        <p>
            {{if ne (index .StringMap "remote_ip") ""}}
                Your remote IP address is {{ index .StringMap "remote_ip"}}
            {{else}}
                Your IP address is not in session. Visit <a href="/">Homepage</a> to set it
            {{end}}
        </p>

      </div>
    </div>
  </div>
{{end}}